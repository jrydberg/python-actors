#!/usr/bin/env python
from distutils.core import setup

setup(name = "python-actors",
      version = "0.2",
      long_description = "Python Actors",
      author='Donovan Preston and Johan Rydberg',
      packages = ['pyact'],
      package_dir = {'pyact': 'pyact'},
    )
